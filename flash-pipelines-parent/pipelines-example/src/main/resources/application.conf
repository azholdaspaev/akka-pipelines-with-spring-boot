pipeline {
  actor.pool.first {
    router = round-robin-pool
    nr-of-instances = 1
    actorType = SIMPLE_ACTOR
    name = first
  }

  actor.pool.second {
    router = round-robin-pool
    nr-of-instances = 1
    actorType = SIMPLE_ACTOR
    name = second
  }

  actor.pool.third {
    router = round-robin-pool
    nr-of-instances = 1;
    actorType = SIMPLE_ACTOR
    name = third
  }

  actor.pool.fourth {
    router = round-robin-pool
    nr-of-instances = 1;
    actorType = ENDING_ACTOR
    name = fourth
  }
}

